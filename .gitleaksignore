# gitleaks allowlist — fingerprints of findings that are known and neutralized.
#
# Legacy Supabase HS256 JWT secret committed to server/.env in bc3fd78 (2025-09-27).
# The secret was permanently REVOKED in the Supabase dashboard on 2026-07-26 as the
# final step of the D-05 legacy API key migration
# (skills/decisions/D-05-supabase-jwt-rotation-readiness.md), so the leaked value can
# no longer mint or verify a token. The string remains in git history at this commit;
# this entry acknowledges the dead secret rather than rewriting history.
bc3fd78f8c822740d6b29bb3eb70167fd335e19f:server/.env:generic-api-key:4

# `sk_test_notarealkey` in withStripeConfigured (server/payments_phase3_test.go).
# Not a secret and never was one: a 19-character literal that spells out what it
# is, assigned to stripe.Key so that handler branches running BEFORE any API call
# can be tested. The helper's own comment says anything that actually talks to
# Stripe belongs in the smoke file behind a real key from the environment.
#
# Fingerprinted rather than allowlisted by path or by rule: a REAL sk_live_/
# sk_test_ key added to this same file, or this same placeholder moving to a
# non-test file, still fails the scan. That is the point — this check was
# skipped entirely until gitleaks was installed, and a scanner muted with a
# broad exclusion is worth no more than one nobody runs.
6cf473e97a554977bee1773c0f79ba6b2b180520:server/payments_phase3_test.go:stripe-access-token:1072

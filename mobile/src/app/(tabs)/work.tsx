import { Text } from "react-native";
import { Screen } from "../../components/ui";

export default function Work() {
  return (
    <Screen headline="Work">
      <Text className="text-body text-muted">Available tasks will show up here.</Text>
    </Screen>
  );
}
